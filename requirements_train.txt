# Mythos v3 — Training dependencies
# Install: pip install -r requirements_train.txt

# Core training stack
torch>=2.3.0
transformers>=4.45.0
peft>=0.12.0
trl>=0.11.0
accelerate>=0.34.0
deepspeed>=0.15.0

# Flash attention (requires CUDA 11.8+ and A100/H100)
# Install separately: pip install flash-attn --no-build-isolation
# flash-attn>=2.6.0

# Data
datasets>=3.0.0

# Utilities
bitsandbytes>=0.44.0
sentencepiece>=0.2.0
protobuf>=4.0.0
packaging>=23.0
safetensors>=0.4.0

# Optional: experiment tracking
# wandb>=0.18.0
