# Mythos v3 — Training dependencies
#
# Install order:
#   1. pip install unsloth                              (pulls in compatible transformers/trl/peft)
#   2. pip install "torch>=2.6.0" \
#        --index-url https://download.pytorch.org/whl/cu124  (torchao 0.17+ needs torch>=2.6)
#   3. pip install -r requirements.txt                  (pin remaining utilities)
#
# NOTE: Unsloth manages its own ecosystem — do NOT pin torch/transformers/trl/peft here
#       to avoid conflicting with what Unsloth expects.

# Data & utilities
datasets>=4.0.0
sentencepiece==0.2.0
protobuf==5.28.3
packaging>=24.0
safetensors>=0.4.5

# Data
datasets==3.1.0

# Utilities
bitsandbytes==0.45.0
sentencepiece==0.2.0
protobuf==5.28.3
packaging==24.2
safetensors==0.4.5

# Optional: experiment tracking
# wandb==0.18.7
