# Mythos v3 — Training dependencies
# Install order:
#   1. uv pip install -r requirements.txt
#   2. pip install unsloth   (handles its own torch/triton deps)

# Core training stack
torch==2.5.1
transformers==4.46.3
peft==0.13.2
trl==0.12.2
accelerate==1.1.1

# Unsloth — installed separately (manages its own CUDA kernel deps)
# pip install unsloth
# unsloth

# Flash attention — bundled inside Unsloth; no separate install needed

# Data
datasets==3.1.0

# Utilities
bitsandbytes==0.45.0
sentencepiece==0.2.0
protobuf==5.28.3
packaging==24.2
safetensors==0.4.5

# Optional: experiment tracking
# wandb==0.18.7
